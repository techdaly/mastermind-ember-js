import { numberColor } from 'mastermind-ember-js/helpers/number-color';
import { module, test } from 'qunit';

module('Unit | Helper | number color');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = numberColor([42]);
  assert.ok(result);
});
