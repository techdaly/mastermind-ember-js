import { blackWhite } from 'mastermind-ember-js/helpers/black-white';
import { module, test } from 'qunit';

module('Unit | Helper | black white');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = blackWhite([42]);
  assert.ok(result);
});
